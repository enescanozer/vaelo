# Cloudflare Pages yapılandırması.
# pages_build_output_dir çıktı dizinini KESİNLEŞTİRİR: panelde yanlış ayarlıysa bile
# (ör. kök) Pages artık repo kökünü (dev index.html + /src kaynağı → kör ekran) DEĞİL,
# derlenmiş dist/'i servis eder. Kök-neden buydu; bu satır tekrarını önler.
name = "vaelo"
pages_build_output_dir = "dist"
compatibility_date = "2024-11-01"

# Pages Function (OG-meta functions/[[yol]].js) RUNTIME değişkenleri.
# NOT: bunlar Function içindir (env.SUPABASE_URL), Vite BUILD'i için DEĞİL —
# istemci VITE_* değerlerini .env.production'dan gömer. Değerler public (anon).
# CF_CODE, Cloudflare Stream kurulunca eklenecek (şimdilik kapak üretilmiyor).
[vars]
SUPABASE_URL = "https://xcrzycuikeoyzbozoayy.supabase.co"
SUPABASE_ANON_KEY = "sb_publishable_m8iiWYA9WhFwQ26t-CFj-A_Y86UlkZv"
